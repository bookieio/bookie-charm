description "uWSGI for Bookie"
start on runlevel [2345]
stop on runlevel [!2345]
respawn
exec /usr/local/bin/uwsgi --socket /tmp/bookie.sock \
    -H /home/bmark.us/0.5 \
    --chmod-socket --module wsgi \
    --pythonpath /home/bmark.us/0.5/bookie \
    -p 8 --threads 2
